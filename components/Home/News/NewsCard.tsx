import React from 'react';

type Props = {
    image: string;
    date: string;
    title: string;
}

const NewsCard = ({date, image, title}: Props) => {
  return (
    <div>
    </div>
  )
}

export default NewsCard
